# Stack Detection Report — Foundry UI Refactor

## Framework
- **React 19** (functional components, hooks-based state)
- **TypeScript** (.tsx files throughout)
- **Vite** build tool with vite-plugin-electron

## Styling Approach
- **Tailwind CSS v3.4** (primary utility framework)
- **CSS custom properties** defined in `src/index.css` (legacy theme tokens)
- **Plain CSS** for shell grid layout and component-specific styles in `src/index.css`
- No CSS modules, no styled-components

## Electron/Tauri Detection
- **Electron 34.2** detected (`electron` in dependencies, `electron/main.ts`, `vite-plugin-electron`)
- Custom titlebar feasible: app uses frameless window architecture with custom shell grid
- Current shell: `.app` grid with `.brand`, `.topbar`, `.side`, `.main` areas

## Current Token System (Legacy)
Located in `src/index.css` and `tailwind.config.js`:
```css
:root {
  --ink:        #17140f;
  --panel:      #1e1a13;
  --panel-2:    #251f16;
  --raised:     #2c2419;
  --line:       #3a3122;
  --line-soft:  #2a2419;
  --text:       #ece3d2;
  --text-dim:   #b5a995;
  --text-faint: #9e927c;
  --copper:     #e0954a;
  --copper-dim: #7a5227;
  --copper-glow: rgba(224,149,74,0.16);
  --good:       #8faa63;
  --warn:       #d9a441;
  --bad:        #c15a3f;
}
```

## Tailwind Config Extensions
- Custom colors: `ink`, `panel`, `raised`, `line`, `foundry`, `copper`, `good`, `warn`, `bad`, `factorio`
- Font families: `slab` ("Zilla Slab"), `sans` (Inter), `mono` (IBM Plex Mono)

## Icon System
- **lucide-react** already installed (no new dependency needed)
- Current usage: inline SVG elements throughout components (NOT using lucide-react yet)

## Key Observations for Refactor
1. **Serif display face** ("Zilla Slab") used for wordmark, mod titles in drawer, section headers — must be replaced with sans per spec §1.4
2. **Native `<select>`** elements used extensively — must be replaced with custom Select component per spec §2
3. **Sidebar footer** with load order bar exists — must be removed per spec §3.5
4. **14-chip tag wrap** in sidebar — must collapse to single "Filters" button + popover per spec §3.5
5. **No custom titlebar** currently implemented — Electron app uses browser-style top area
6. **Scrollbars**: Currently themed but thumb is orange-ish — must change to `--border-strong` per spec §1.10
7. **Focus rings**: Not consistently applied — need global `:focus-visible` treatment per spec §1.9

## File Structure Summary
```
/workspace
├── src/
│   ├── App.tsx              (main app shell, state holder)
│   ├── index.css            (global styles, legacy tokens, shell grid)
│   ├── components/
│   │   ├── Topbar.tsx       (profile pill, search, actions)
│   │   ├── Sidebar.tsx      (categories, filters, footer)
│   │   ├── InstalledView.tsx (mod list + detail rail)
│   │   ├── OnlineView.tsx   (discover catalog + detail rail)
│   │   ├── ProfilesView.tsx (profile management)
│   │   ├── DependenciesView.tsx (dependency health table)
│   │   ├── SettingsModal.tsx (paths, downloads, launch settings)
│   │   └── ... (Modals, DownloadBar)
│   └── primitives/          (TO BE CREATED by A1)
├── tailwind.config.js       (theme extensions)
└── docs/ui-refactor/        (refactor documentation)
```

## Migration Strategy
1. Replace legacy CSS tokens with new spec tokens in `src/index.css`
2. Mirror tokens into `tailwind.config.js` for utility class usage
3. Build primitive components in `src/primitives/` directory
4. Incrementally replace view components while preserving all event handlers and state wiring

---
*Generated by A0 "recon" agent — Phase 0*
